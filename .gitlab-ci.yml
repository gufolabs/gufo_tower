before_script:
- docker info

build:
  stage: build
  script:
    - docker run python:2.7.11-alpine -v .:/app python setup.py sdist format=targz
  artifacts:
    paths:
      - sdist/*


#    - env|sort
#    - export VERSION=$(cat VERSION)
#    - ls -lar
#    - docker build -t nocproject/tower:${VERSION} .