image: docker:latest

services:
- docker:dind

before_script:
- docker info

build:
  stage: build
  script:
    - env|sort
    - export VERSION=$(cat VERSION)
    - docker build -t nocproject/tower:${VERSION} .