build:
  stage: build
  image: python:2.7.11-alpine
  script:
    - apk add --update py-pep8 py-setuptools
    - pep8 --ignore=E265,E266,E501,402 .
    - python setup.py sdist --format=bztar
  artifacts:
    paths:
      - dist/*
  after_script:
    - env |sort
  tags:
    - dind


#    - env|sort
#    - export VERSION=$(cat VERSION)
#    - ls -lar
#    - docker build -t nocproject/tower:${VERSION} .