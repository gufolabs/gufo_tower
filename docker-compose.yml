version: '2'
services:
  tower:
    image: registry.gitlab.com/nocproject/tower:master
    restart: always 
    ports:
      - "8888:8888"
    volumes:
      - "/opt/tower/var:/opt/tower/var/"
      #https://groups.google.com/forum/#!topic/ansible-project/y8ohlv_dRi4
      - "./root:/root"
