# Getting Started

After installing Gufo Tower, open the Tower web interface in your browser.

Use the address of the Tower server followed by port **8888**:

```text
http://<tower-address>:8888/
```

Replace `<tower-address>` with the hostname or IP address of the server running Gufo Tower.

